<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="DrageeScales.Shared.Controls.OverlapModalProgressView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DrageeScales.Shared.Controls"
    xmlns:local2="using:DrageeScales.Shared.Dtos"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:DataType="local2:ProgressModalOption">

    <Grid Visibility="{x:Bind BaseOption.IsModalVisible,Mode=OneWay}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="Progress">
                    <VisualState.Setters>
                        <Setter Target="ProgressRing.Visibility" Value="true"></Setter>
                        <Setter Target="ProgressPercent.Visibility" Value="true"></Setter>
                        <Setter Target="BusyRing.Visibility" Value="false"></Setter>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Busy">
                    <VisualState.Setters>
                        <Setter Target="ProgressRing.Visibility" Value="false"></Setter>
                        <Setter Target="ProgressPercent.Visibility" Value="false"></Setter>
                        <Setter Target="BusyRing.Visibility" Value="true"></Setter>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Rectangle Fill="LightGray" Opacity="{x:Bind BaseOption.Opacity,Mode=OneWay}" ></Rectangle>
        <StackPanel x:Name="StatePanel" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ProgressRing x:Name="ProgressRing" IsIndeterminate="false" Width="124" Height="124" Value="{x:Bind ProgressModalOption.ProgressValue,Mode=OneWay}"></ProgressRing>
            <ProgressRing x:Name="BusyRing" IsIndeterminate="{x:Bind BusyModalOption.IsBusy}" Width="124" Height="124"></ProgressRing>
            <StackPanel x:Name="ProgressPercent" Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Center">
                <TextBlock FontSize="24" Text="{x:Bind ProgressModalOption.ProgressValue,Mode=OneWay}"></TextBlock>
                <TextBlock FontSize="16" Text=" %"></TextBlock>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
