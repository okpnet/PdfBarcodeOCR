<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="DrageeScales.Views.Controls.FileItemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DrageeScales.Views.Controls"
    xmlns:viewDtos="using:DrageeScales.Views.Dtos"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:DataType="viewDtos:PdfPageAdpter">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Width="512" Margin="4,4,4,4">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Image Source="{x:Bind PdfAdapter.ThumbnailImageSource,Mode=OneWay}" Height="{x:Bind TumbnailSlideRate,Mode=OneWay}" Width="{x:Bind TumbnailSlideRate,Mode=OneWay}" Stretch="UniformToFill" VerticalAlignment="Center"></Image>
            </Grid>
            <StackPanel Grid.Column = '1' Margin="8,0,0,8" Orientation="Vertical" VerticalAlignment="Center">
                <StackPanel Orientation = 'Vertical'>
                    <TextBlock FontSize="12" Text = '保存ファイル名' Style = '{ThemeResource CaptionTextBlockStyle}'/>
                    <TextBox Margin="4,4,4,4" IsEnabled="{x:Bind PdfAdapter.IsEnabeled,Mode=OneWay}" Text = '{x:Bind PdfAdapter.FileNameToSave,Mode=TwoWay}' Style = '{ThemeResource CaptionTextBlockStyle}'/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="4,4,4,4">
                    <HyperlinkButton IsEnabled="{x:Bind PdfAdapter.IsEnabeled,Mode=OneWay}" Click="HyperlinkButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <FontIcon Glyph="&#xE73E;" />
                            <TextBlock Text="保存"></TextBlock>
                        </StackPanel>
                    </HyperlinkButton>
                </StackPanel>
            </StackPanel>
        </Grid>
        <ProgressBar Value="{x:Bind PdfAdapter.ProgressValue,Mode=OneWay}" IsIndeterminate="{x:Bind PdfAdapter.IsBusyBar,Mode=OneWay}" ShowError="{x:Bind PdfAdapter.IsBarcodeReadFail,Mode=OneWay}" ShowPaused="False"></ProgressBar>
    </StackPanel>
</UserControl>
